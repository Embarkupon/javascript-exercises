things that we need to do.
return the sum of all numbers between a min and a max inclusively.
get the 'n' count of consecutive numbers, and find the sum of
consecutive integers using the formula (n/2) * (min + max) where n = max-min+1;

function with paramaters min and max
return ((max-min+1)/2) * (min+max)
